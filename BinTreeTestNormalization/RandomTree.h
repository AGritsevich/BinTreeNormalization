#include "stdafx.h"
//#include<cstdlib>
//#include<ctime>
//#include <vector>
//#include <algorithm>
//#include <iostream>
//#include <ostream>
//#include <functional>
/*
struct TreeNodeV1
{
	float value;
	TreeNodeV1 *left;
	TreeNodeV1 *right;
	int weight;

	TreeNodeV1() : left(NULL), right(NULL), weight(1) {};
	TreeNodeV1(float _val) : left(NULL), right(NULL), weight(1)
	{
		value = _val;
	}
};

class CSearchBinTreeHelperV1
{
private:
	TreeNodeV1 *m_root;
	int MAX_N;

private:
	int GetWeight(TreeNodeV1* node);
	void SetWeight(TreeNodeV1* node);
	TreeNodeV1* RotatetoRight(TreeNodeV1* M);
	TreeNodeV1* RotateToLeft(TreeNodeV1* N);
	TreeNodeV1* InsertRoot(TreeNodeV1* node, float val);
	TreeNodeV1* Insert(TreeNodeV1* node, float val);
	TreeNodeV1* Find(TreeNodeV1* node, float val);

public:
	CSearchBinTreeHelperV1():m_root(NULL), MAX_N(1000) {};
	~CSearchBinTreeHelperV1(){};
	
	int _print_t(TreeNodeV1 *tree, int is_left, int offset, int depth, char s[30][255]);
	void print_t(TreeNodeV1 *tree);

	TreeNodeV1 *generate_tree( std::vector<float> nums);
};
*/